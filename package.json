{
  "name": "tenzies",
  "dependencies": {
    "nanoid": "^3.2.0",
    "react": "17.0.2",
    "react-confetti": "^6.0.1",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.14"
  },
  "scripts": {
    "build": "cp index.html style.css build && esbuild --bundle index.jsx --outfile=build/index.js",
    "serve": "python3 -m http.server --directory build",
    "watch": "cp index.html style.css build && esbuild --bundle index.jsx --outfile=build/index.js --watch"
  }
}
